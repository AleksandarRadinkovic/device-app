import { FilterDevicesPipePipe } from './filter-devices-pipe.pipe';

describe('FilterDevicesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDevicesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
